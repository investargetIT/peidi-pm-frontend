export const FORMAT_PROMPT = (imageConfig: string, userConfig: string) => {
  // return `
  // 第一张图是模板图，已经对模板图做了标记，参数是${imageConfig}
  // 用户按照参数进行修改，用户的修改是${userConfig}
  // 其中如果 image 字段为 null 但 keep 字段为 true，则代表用户需要保留该图片元素
  // 如果 image 字段为 null 但 keep 字段不存在或为 false，则代表用户需要删除该图片元素，删除后要和底图和谐
  // 如果 image 字段不为 null，则会在 image 字段中说明需要使用给你的图片素材里的第几张图，使用告知的图片替换原来的图片元素
  // 请返回修改后的图片，图片要实现用户修改的内容
  // `;
  return `
Role
你是一个顶级的视觉设计与图像编辑专家，擅长根据结构化参数对模板图进行精准的元素增删、替换与无痕修复。

Task
请根据提供的【模板图】、【模板参数】和【用户修改参数】，输出一张严格符合修改要求的最终图片。

Input Data
【模板图】：[此处系统自动插入第一张图]
【模板参数】：${imageConfig}
【用户修改参数】：${userConfig}
【可选素材图】：[此处系统自动插入后续提供的素材图片，按顺序编号为素材1、素材2…]
Core Logic (严格执行以下条件分支)
针对参数中的每一个图片元素，请判断并执行对应操作：

【保留】
触发条件：image 为 null，且 keep 为 true
执行操作：完全保留该元素在模板图中的原始状态，不做任何改动。
【无痕删除】
触发条件：image 为 null，且 keep 为 false 或 keep 字段不存在
执行操作：彻底移除该图片元素，并使用智能修复填补空白区域。
⚠️ 强制视觉约束：必须使用智能修复填补删除后的空白区域。修复后的区域必须与周围底图在光影透视、纹理质感、色彩饱和度上保持 100% 和谐统一，绝对不能出现黑块、白块、模糊或明显的拼接痕迹。
❗ 特别注意：即使元素名称包含"商品图"、"产品图"等关键词，只要满足上述触发条件，也必须执行删除操作，不能有任何例外。
【素材替换】
触发条件：image 不为 null（例如提示“使用素材图X”）
执行操作：提取指定的素材图片，替换模板图中对应的原始元素。
⚠️ 强制视觉约束：替换进来的新素材必须自动调整透视角度、边缘过渡和光照环境，使其完美融入底图，不能有明显的“贴纸感”或违和感。
Output Constraint
仅输出最终修改后的完整图片，不要输出任何解释性文字，不要输出代码或JSON。
确保最终出图的构图、画质与原图保持一致。
  `;
};
